{
  "routes": [
    {
      "method": "GET",
      "path": "/coinrating",
      "handler": "CoinRating.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/coinrating/:_id",
      "handler": "CoinRating.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/coinrating",
      "handler": "CoinRating.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/coinrating/:_id",
      "handler": "CoinRating.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/coinrating/:_id",
      "handler": "CoinRating.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
